super + z
	notify-send Time $(date '+%H:%M')

super + s
	notify-send Battery $(upower -i $(upower -e | grep 'BAT') | grep -E 'percentage' | cut -d ' ' -f15)

alt + Tab
	rofi -show window

XF86AudioMute
	pulseaudio-ctl mute

super + Return
	gnome-terminal

super + Shift + Return
	rofi -show run
